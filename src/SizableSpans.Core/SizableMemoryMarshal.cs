using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Text;

namespace Zyl.SizableSpans {
    /// <summary>
    /// Provides a collection of methods for interoperating with <see cref="Span{T}"/>, and <see cref="ReadOnlySpan{T}"/>. It can be regarded as the <see cref="MemoryMarshal"/> of <see cref="UIntPtr"/> index range (提供与 SizableSpan 和 SizableReadOnlySpan 互操作的方法. 它可以被视为 UIntPtr 索引范围的 MemoryMarshal).
    /// </summary>
    public static partial class SizableMemoryMarshal {

    }
}
